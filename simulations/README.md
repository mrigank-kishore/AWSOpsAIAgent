# Simulations

This folder should contain reproducible incident triggers for:

- CPU spike
- memory leak
- service crash / 5xx spike

Each simulation should be runnable in local mode and produce telemetry that mirrors the AWS target architecture.
